#include <stdlib.h>
#include <math.h>

int main()
{
    int size, i, j;
    scanf("%d", &size);
    int set[size];

    for (i = 0; i < size; i++)
        scanf("%d", &set[i]);
    printf("\nNull \n");

    for (i = 0; i < size; i++)
    {
    }
}